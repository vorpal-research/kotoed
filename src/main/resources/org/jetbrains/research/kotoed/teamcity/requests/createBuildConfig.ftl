<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<buildType id="${build.id}" name="${build.name}">
    <project id="${project.id}" name="${project.name}"/>
    <template id="${build.templateId}"/>
    <vcs-root-entries count="1">
        <vcs-root-entry>
            <vcs-root id="${vcs.id}" name="${vcs.name}"/>
            <checkout-rules></checkout-rules>
        </vcs-root-entry>
    </vcs-root-entries>
</buildType>