extends ./base

include elements/navBar
include elements/breadCrumb

append content
    +makeNavbar(context.navbar)
    +makeBreadcrumb(context.breadcrumb)
    div(id="kotoed-notification-container")
